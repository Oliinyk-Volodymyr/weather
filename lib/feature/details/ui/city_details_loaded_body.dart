import 'package:flutter/material.dart';

class CityDetailsLoadedBody extends StatelessWidget {
  const CityDetailsLoadedBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
